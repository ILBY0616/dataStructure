int main()
{
    // int top = 0, lTop = 0, rTop = 0, data = 0, size = 0;
    // int stack[100];
    // // 顺序栈
    // top = -1;
    // // 栈空
    // top == -1;
    // // 栈满
    // top == size - 1;
    // // 读栈
    // data = stack[top];
    // // 入栈
    // stack[++top] = data;
    // // 出栈
    // data = stack[top--];
    //
    // // 共享栈
    // lTop = -1, rTop = size;
    // // 栈空
    // lTop == -1, rTop == size;
    // // 栈满
    // lTop + 1 == rTop;
    // // 读栈
    // data = stack[lTop];
    // data = stack[rTop];
    // // 入栈
    // stack[++lTop] = data;
    // stack[--rTop] = data;
    // // 出栈
    // data = stack[lTop--];
    // data = stack[rTop++];
    return 0;
}
