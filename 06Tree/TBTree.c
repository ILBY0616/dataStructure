#include "TBTree.h"

// 头结点：
// 左指根节点或头结点
// 右指遍历序列结束结点或头结点
// 非头结点：
// 左指左孩子（lTag==false）或遍历序列前驱结点（lTag==true）或头结点（遍历序列开始结点）
// 右指右孩子（rTag==false）或遍历序列后驱结点（rTag==true）或头结点（遍历序列结束结点）

int main()
{
    return 0;
}
